import {NativeModules} from "react-native";


var ToastExample = NativeModules.ToastExample;


const gertecCamV1 = {
  codeBarEan8: async function () {
    return await ToastExample.startCameraV1("EAN 8");
  }
}

export default gertecCamV1;

/*
import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  Button,
  StatusBar,
  TouchableOpacity
} from 'react-native';

import {
  Header,
  LearnMoreLinks,
  Colors,
  DebugInstructions,
  ReloadInstructions,
} from 'react-native/Libraries/NewAppScreen';

import {NativeModules} from 'react-native';

var ToastExample = NativeModules.ToastExample;

const App: () => React$Node = () => {


 

  return (
    <>
      
      <SafeAreaView>
        <View>
          <Text style={{fontSize: 30, textAlign: 'center', fontWeight: 'bold', height: 50}}>CodeBar EAN 8</Text>
        </View>
    
        <View style={styles.fixToText}>        
           
          <Button title="EAN 8" onPress={() => ToastExample.startCameraV1("EAN 8") } />                     
          
        </View>

        <TouchableOpacity style={{height: 96 ,flexDirection:"row",alignItems:'center',justifyContent:'center'}} onPress={() => ToastExample.startCameraV1("EAN 8")}>
              

              <Text style={{flex:.8, fontSize: 15, textAlign: 'center'}}>Código de Barras V1</Text>      
             
          </TouchableOpacity>
       
        <View style={{ height: 0.5, width: "100%", backgroundColor: "#000" , marginTop: 20}} />          
        
      </SafeAreaView>  

    </>
  );
};

const styles = StyleSheet.create({
  fixToText: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    height: 30,
    marginStart: 15,
    marginEnd: 15,
  },
  item: {
    flexDirection: 'row',
  },
});

export default App;
*/